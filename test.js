const valLength = (val) => {
  console.log("valleng is ==", val);
};

const checkBool = () => {
  console.log("testing bool");
};

// default export
module.exports = {
  valLength,
  checkBool,
};

// exporting as default
